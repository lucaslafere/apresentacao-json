import { Link } from 'react-router-dom';
import { Container, Button } from './MainScreen';
import styled from 'styled-components';

export default function CssStyleLoaderScreen () {
    return (
        <div>O que são o CSS Loader e o Style Loader do Webpack e como usá-los (sem Create React App)?</div>
    )
}