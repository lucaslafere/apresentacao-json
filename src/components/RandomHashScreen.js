import { Link } from 'react-router-dom';
import { Container, Button } from './MainScreen';
import styled from 'styled-components';

export default function RandomHashScreen () {
    return (
        <div>Por que as imagens processadas pelo Image Loader contém um hash aleatório no nome? (dica: tem a ver com Cache do navegador!)</div>
    )
}