import { Link } from 'react-router-dom';
import { Container, Button } from './MainScreen';
import styled from 'styled-components';

export default function ImageLoaderScreen () {
    return (
        <div>O que é o Image Loader do Webpack e como usá-lo (sem Create React App)?</div>
    )
}